spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bookshop?userUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: admin
mybatis:
  type-aliases-package: pers.hyy.bookshop.entity
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8899
